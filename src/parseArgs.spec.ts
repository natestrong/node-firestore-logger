import {_convertTokenToCollection} from "./parseArgs";

describe('parseArgs', () => {
    // it('convertTokenToCollection should split a string and return an ICollection object', () => {
    //     expect(_convertTokenToCollection('/users')).toEqual({path:'/users', queries: null})
    // })
    it('testing..', () => {
        expect(true).toBeTruthy();
    });
});
